{
  "buildCommand": "pnpm build-storybook && mkdir -p public/storybook && cp -r storybook-static/* public/storybook/ && tsx scripts/fix-storybook-paths.ts && pnpm registry:build && pnpm build",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "outputDirectory": null,
  "headers": [
    {
      "source": "/storybook/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
